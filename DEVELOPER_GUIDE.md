# Developer Guide

This project serves as a starting point for creating browser extensions with WXT.

## Best Practices

- Keep extension logic modular and testable.
- Use composables for reusable logic.
- Write unit and end-to-end tests for features.
- Keep configuration in the `wxt.config.ts` file.

## Environment Variables

Copy `.env.example` to `.env` and adjust values as needed.
